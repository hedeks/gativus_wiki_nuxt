- Нет адаптации админ панели на мобильные устройства она не полная.
- Разногласие стилей, разные кнопки и цвета моментами -> Подробный план по устранению вынесен в `plans/design_consistency_plan.md`.
- У терминов должна быть картинка и презентация, в том числе и возможность видео.
- Надо подумать над безопасностью.
- В будущем хочется иметь возможность работать сразу с графом знаний: подгружать из odt, смотреть в preview статью, так же иметь возможность в html открыть, это все должно быть как надвигающимся окном, т.е функции какие должны быть: 1) добавить термин, статью, книгу, категорию, т.е. это все узлы графа, 2) добавить отношения между узлами, т.е. ассоциировать категорию с термином, статьей, книгой, а остальные виды отношений строго в админке, удаление и редактирование в админке, а вот добавление связей с категориями уже сразу на этой страничке.
- Импорт и экспорт в виде json графа. // остались ассеты
- Потом при полировке надо подумать над оптимизацией и провести тест со сбилженным сайтом.

- В админке при добавлении odt нужно еще и выбор языка добавить

- В админке еще должно быть универсальное некое дерево которое позволяет при редактировании термина создать с нуля для него статью по тем же канонам что и в админке при создании статьи, но сайт должен помнить куда нужно вернуться, т.е. быть неким универсальным редактором.

- Не позволяет создать термин и вообще в страничке создания термина нужно зарефакторить: 1) три языка: английский основной, название на английском. Ошибка: the form element is not focusable.

- Весь сайт нужно зарефакторить на поддержку 3-х языков для интерфейса, в том числе граф не перерендеривается для другого языка в текущем состоянии, а должен так как на другом языке другие статьи.

- Добавление индексации для сайта по канонам opendocument, т.е. список 1,2,3, а так же сделать возможно загрузить мастер документ, и соответственно надо его распарсить узнать сколько статей нужно загрузить и пока пользователь их все не подгрузит, т.е. соединит ссылку с ресурсом для каждого, тогда можно уже добавлять это все на сайт.

- Поиск в статьях не работает.

- Сдлеать у инпутов шрифт 16 px или же ограничить приближение в самом html <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">


// 28.04.2026

[] Остается фикс визуальных багов в админ панели.
[] Добавление odm документа и качественного редактора статьи.
[] Фикс различия стилей и унификация интерфейса.
[] Оптимизация и безопасность.
[] Сделать чтобы попап открывался со скелетоном даже без текста.
[] На графе закрывать попап только при клике, а не при mousedown.
[] Сделать главную страничку с parallax эффектом, которая будет рассказывать чем занимается проект, чтобы было без лагов даже в firefox!
[] Добавить в json export количество упоминаний.
[] Адаптировать странички books/slug под новую систему дизайна и articles/slug.
[] Починить TOC и почему некоторые ссылки не отрабатывают вовсе.