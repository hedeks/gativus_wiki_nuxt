1. [x] доделать полную локализацию, на страницах books/slug там названия книг и описание только на английском.
2. [x] при загрузке ODM я должен иметь возможность менять название и загружать odt файлы, не нажимая кнопку сохранить названия, тоесть как бы я сначала заполняю модель а потом она сохраняется вместе с подгрузкой odt, так же нужно добавить возможность подгрузить другой odt если что, тоесть загруженный odt отменить.
3. [x] добавить в admin нормальный редактор html -> preview иметь возможность свапать представления. возможно добавить кастомный редактор, но без доп библиотеки чисто java скриптовый.
4. [x] нужна возможность того, что можно из odm не все главы подгружать только желаемые, например не подгрузить 6-ю главу по желанию.
5. [] добавить возможность книгу как pdf скачивать. // не трогаем
6. [x] при редактировании итема в глоссарии нужна возможность видеть preview попапа, который будет показываться.
7. [x] при создании статьи из odt термины внутри оборачиваются в wiki-link, но мне не нравится, что для графа связи почему - то не образуются.
8. [x] при создании книги все odt автоматически попадают в базу с атрибутом is_published: 0, но при отмене создании книги они там и остаются это паразитное поведение, нужно чтобы они удалялись если книгу я не создал все-таки.
9. [x] в safari pdf viewer не открывается на весь экран, мне это не нравится и ещё в принципе не подгружается пдф.
10. [] не продумана онтологическая роль категорий, хотя она изначально задумывалась.
11. [x] articles-prose.css: не продуман визуальный вид code и pre блоков, pre должен быть по центру и как некий едва ли выделяющийся блок с моно шрифтом.
12. [x] слева в менюшке, где написано Текст: блаблабла, там нужно добавить какую - то мета информацию о том какая книга это вообще.
13. [x] мне не нравится как адаптировано theToc на мобильный телефон наверное лучше его как блок на всю ширину и прижатым к header чтобы он занимал меньше пространства и не мешался или еще лучше чтобы header открывался при нажатии на кнопку а содержание и было header-ом но лого Gativus я бы хотел так же оставить в header.
14. [x] что касается стилей articles-prose: мне хочется, чтобы в таблице могли переноситься многословные предложения по словам, но не по буквам.
15. [x] выравнивание в таблицах должно быть не по центру а у левого края с небольшим отступом.
16. [x] слева в articles/slug я хочу видеть навигацию по главам книги.
17. [x] возможность удалять книгу вместо со статьями внутри, тоесть в popup спросят удалить ли статьи вместе с книгой или нет?


// new 17.05.26

1. [x] В админке добавить промт для перевода сразу на все языки, исходя из исходного, для оптимизации.

2. [x] Добавить возможность в админке articles/edit при редактировании для каждого языка отдельно парсить odt и смореть что получается сразу в preview.

3. [x] Для попапов возможность парсить bold разметку текста.

4. [x] Надпись предыдущая глава и следующая глава на русском и не связаны с языком.

5. [x] Footer так же не мультиязыковый.
6. [x] Кнопка ScrollToTopButton не мультиязыковая.
7. [x] На страницах поиск и фильтрация так же не мультиязыковые.
8. [x] theProfileIcon не мультиязыковый.
9. [] На главной страничке так же не все тезисы мультиязыковые.
10. [x] В админке у articles не хватает пометки или флага, если можно так выразиться о наличии всех языков, вместо надписи model - Global, для того чтобы легче было переводить.
11. [] Можно ли сделать так чтобы в графе знаний не могли пересекаться связи между узлами одного уровня, т.е. и еще элементы не должны находится под или над связями если это возможно? или это полный бред?
12. [x] Оптимизация админ панели и адаптирование на мобильные устройства.
13. [x] Сделать нормальный редактор в preview админки.
14. Экспорт json и import json должен сопровождаться архивом с assets.zip или любой другой удобный формат архивирования, т.е. все cover pages и другие assets лежащие в server/storage и *ключевое* используемые в самом дампе в json должны экспортироваться и потом импортироваться. т.е. формат экспорта два файла, json и assets к этому json, чтобы избегать коллизии имен наверное лучше всего и assets так же к дате привязывать.
15. [x] В glossary/[slug] мобильная версия должна работать так же как в articles/[slug] — адаптивный theToc, поведение хедера и т.д. Также в мобильной версии theToc отсутствует кнопка "Перейти к презентации".
16. [x] А так же в графе знаний хочу добавить эффект какого то плавного появления и исчезания элементов при приближении, а так же нужно пофиксить чтобы при нахождении элемента он был проявлен так как сейчас при поиске термина на далеком уровне дальности его не видно, а так же нужно добавить поиск по EGO графу, чтобы search работал и в ego графе, и это все нужно внести в задачи а потом пометить как выполненные

17. [x] ODT-парсер: в edge-case когда text:span с code-стилем содержит вложенный text:p с preformatted-стилем — получается <code><pre><code>...</code></pre></code>. Фикс: в span-хендлере (odtParser.ts:661) пропускать <code>-обёртку если result уже начинается с <pre.

18. [x] По какой - то магической причине в браузерах не являющимися firefox есть очень странный баг с постоянной перерисовкой knowledge-graph, и миганием, по ощущениям как будто стоит ограничение на стек памяти, потому что когда элементов ограниченное количество, например на эго-графе то все хорошо, возможная причина: нехватка памяти из - за обилия элементов, причем в firefox удивительно все хорошо, причем в safari на телефоне все отлично работает.

19. [x] Не работает по какой то причине адаптивность изображений на малых экранах в просмотре статей, предположительно причина в @article-prose.css
20. [x] Добавить для всех описаний и попапов поддержку italic и bold и подумтаь может можно даже ссылки.
21. [x] Кнопка "назад в глоссарий" очень кривая 
22. [] Добавить для синонимов разделение по языкам, но тогда наверное придется менять алгоритм перелинковки.
23. [x] На ios не загружается pdf по какой то неведомой причине.
24. [x] При нажатии кнопки "Назад" как поведение браузера должно на статью перекидывать, помня позицию скролла.
25. [] Импорт кусочками json, который вольется в общую базу, так же в админке должен быть пример такого json или некий scheme.
26. [] Добавить возможность делать в статьях ссылки на книги а не только на термины, и должен открываться попап для книг.
28. [x] Страница /books (библиотека) — книги отображаются как карточки с обложкой; редизайн /books/[slug]: чище герой без тяжёлой панели, правильные тени обложки, убрана центровка, добавлен ZH, исправлены SEO-фолбеки.
29. [x] Breadcrumbs на странице /glossary/[slug]: добавлена категория в цепочку (GLOSSARY → Category → Term), исправлены SEO-теги и сообщение об ошибке на EN/ZH., названием и описанием; сетка 2 колонки на десктопе, 1 колонка на мобильном.
27. [] Граф знаний — performance оптимизации при большом объёме узлов: (a) viewport culling — скрывать узлы и рёбра за пределами видимой области при zoom/pan; (b) canvas-слой для рёбер вместо SVG path; (c) замена paint-order stroke/fill на rect-подложку под лейблы.

// new 24.05.26

30. [x] Редизайн /books/[slug]: герой + оглавление объединены в одну gv-surface-card, gv-card-header как разделитель, убран TOC-badge, добавлен ZH, SEO-фолбеки мультиязычные, тени обложки как в BookCard, убрана центровка текста.
31. [x] TheHeader — рефакторинг: убраны иконки из десктопного нава (остались только в мобильном drawer), все action-элементы (поиск, язык, тема, профиль) приведены к единому ghost-стилю 34px с uniform border-radius var(--gv-radius-control), LanguageSwitcher в десктопе compact, theThemeChanger переписан с UToggle на GvButton ghost icon.
32. [x] LandingBlockHero: заголовок переведён на глобальный gv-hero-gradient (консистентно с остальным приложением), упрощён фон карточки (убран backdrop-filter), немного уменьшена min-height.
33. [x] ДОбавить в парсинг odt параметр на каком языке будут подписываться главы и номер главы в книге для индексации не с 1 а например с 3 и соответственно везде где есть возможность импортировать odt нужно это выбирать, так же хочется в самом source коде иметь кнопочку, переиндексировать исходя из параметра номера главы и язык подписи odt-heading-marker.
34. [] Добавить весь функционал из редактирования статьи в создание статьи, я бы даже сказал возможно стоит их обьединить и вынести в единую сущность, так как по сути создание статьи то же самое функционально, что и редактирование просто с пустыми полями, тоже самое и с терминами и с категориями.
35. [] На страничке с терминами в админке добавить возможность экспортировать в json, и например выбирать что экспортируем термины с 15 по 150, для того чтобы работать с ними в json формате удобном.
36. [] Поиск на страницах books/index и articles/index и glossary/index идет не по всем pages, а только на текущей статье странице пагинации, а должен находить по всем.
37. [] Мне не нравится предпросмотр картинки, нужно найти какой нибудь opensource js viewer и скопировать его функционал!!! а так же чтобы он был адаптивным на мобильные экраны. 
38. [x] При нажатии следующая/предыдущая глава в articles/slug я хочу чтобы якорь скидывался и положение странички было со старта, тоесть я хочу чтобы оно возвращало на якорь только когда я нажимаю некий смысловой "back", а когда открываю это главу хочется с нуля.
39. [x] Переделать визуально кнопки пагинации, по цветам все хорошо, мне не нравится их форма и дизайн, не соответствует премиальному стилю @docs/design/design_system.md
40. [] Импорт odm для трех языков, для того чтобы подхватывались оттуда названия и индексы, а так же сразу легко было бы загрузить всю книгу на языке, т.е. так как статья и книга одна сущность универсальная на 3х языках, у неё есть 3 проявления: русский, английский, китайский. 3 odm файла.


TASKofFUTURE: Крупная задача: добавить возможность в авторизированном состоянии при ролях editor и admin редактировать буквально сайт на ходу не заходя в админку, если что поменять описание или текст статьи, в тексте статьи например при соотвествующей роли появляется плавающая кнопка редактирования текста и редактор такой же как в превью в админке, так же возможность редактировать попап текст просто при вызове его, но только с компьютера.
TASKofFUTURE: необходимо, чтобы на графе знаний была возможность добавлять узлы и нажимать кнопку например "перелинковать узел" конкретный, но это когда редакторы в админке будут доработаны, т.е. будет такая админка прямо буквально в sidebar выдвигаться слева на графе знаний для user с ролью admin или editor. Добавление узлов идет по всем канонам того, как это происходит в админ - панели.