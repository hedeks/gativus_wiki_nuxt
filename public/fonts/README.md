Place licensed Gill Sans font files here for self-hosting:

- `GillSans.woff2`
- `GillSans.woff`
- `GillSans-Bold.woff2`
- `GillSans-Bold.woff`

The app uses `@font-face` in `app.vue` and falls back to system fonts if these files are missing.
